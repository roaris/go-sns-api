version: "3"
services:
  app:
    build: .
    tty: true
    ports:
      - "8080:8080"
    volumes:
      - ./app:/app
  db:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD
      - MYSQL_DATABASE
      - MYSQL_USER
      - MYSQL_PASSWORD
    volumes:
      - data-dev:/var/lib/mysql
    container_name: db-container

volumes:
  data-dev:
    driver: local
